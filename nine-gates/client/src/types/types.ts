export interface Skill {
  title: string;
  description: string;
  skillType?: string;
  uri?: string;
}
export interface Occupaition {
  alternativeLabel: string[];
  preferredLabel: string;
  title: string;
  _id: string;
}
